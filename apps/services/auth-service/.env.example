# === Base de données PostgreSQL ===
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
# (Optionnel si vous préférez construire DATABASE_URL)
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# === JWT ===
JWT_SECRET=

# === Serveur HTTP ===
PORT=

# === OAuth Google ===
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=


# === Autres variables éventuelles non présentes ===
# REDIS_URL=
# NEXTAUTH_URL=
